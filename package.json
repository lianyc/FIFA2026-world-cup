{
  "name": "worldcup-2026",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && npx vercel --prod",
    "deploy:github": "npm run build && npx gh-pages -d dist --dotfiles --nojekyll -r git@github.com:lianyc/FIFA2026-world-cup.git"
  },
  "dependencies": {
    "element-plus": "^2.9.1",
    "pinia": "^2.2.6",
    "vue": "^3.5.13",
    "vue-router": "^4.5.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "gh-pages": "^6.3.0",
    "typescript": "~5.6.3",
    "vite": "^6.1.0",
    "vue-tsc": "^2.2.0"
  }
}
