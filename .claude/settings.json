{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "cd x:/bigsail/dsTest && git add -A && if git diff --cached --quiet; then exit 0; fi && file=$(python -c \"import sys,json; d=json.load(sys.stdin); print(d.get('tool_input',{}).get('file_path','unknown').replace('x:/bigsail/dsTest/',''))\" 2>/dev/null || echo \"files\") && git commit -m \"update: ${file:-files}\" && git push 2>/dev/null || true",
            "timeout": 60
          }
        ]
      }
    ]
  }
}
